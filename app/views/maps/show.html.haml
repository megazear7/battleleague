.text-center
  %h2= @map.name
  %p
    = @map.description
    %br
    - if @map.game
      The game that this map is apart of is
      = link_to @map.game.name, @map.game

= map @map

= test @map do |space|
  %span.space{ id: "#{space.x_cord+1}-#{space.y_cord+1}", style: "width: #{500/map.width}px; height: #{500/map.height}px"}
    = space.terrain
  - if space.x_cord == @map.width
    %br

= link_to 'Back', maps_path, class: "btn btn-link"
