%h1 Listing armies

%table.table.table-hover.table-responsive
  %tr
    %th Name
    %th Army Type
    %th Point Cost
    %th Game

  - @armies.each do |army|
    %tr
      %td= link_to army.name, army, class: "btn btn-link"
      %td= army.army_type
      %td= army.point_cost
      %td= link_to army.game.name, army.game, class: "btn btn-link"

%br

= link_to 'New Army', new_army_path, class: "btn btn-link"
