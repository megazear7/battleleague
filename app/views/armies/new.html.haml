- if params[:game_id]
  %h1 
    New army for 
    = Game.find(params[:game_id]).name
- else
  %h1 New army
= render 'form'

%br
- if params[:game_id]
  = link_to 'Back', game_path(params[:game_id]), class: "btn btn-default"
- else
  = link_to 'Back', armies_path, class: "btn btn-default"
